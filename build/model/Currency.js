export class Currency {
    constructor(chainId, name, symbol, decimals) {
        this.chainId = chainId;
        this.name = name;
        this.symbol = symbol;
        this.decimals = decimals;
    }
}
//# sourceMappingURL=Currency.js.map