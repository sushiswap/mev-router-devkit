import { BigNumber } from 'ethers';
export declare function normalizeTo18Decimals(amount: BigNumber, decimals: number): BigNumber;
export declare function denormalizeFrom18Decimals(amount: BigNumber, decimals: number): BigNumber;
